{
  "name": "cnbuycha-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build-check.mjs",
    "dev": "wrangler dev --config wrangler.local.jsonc",
    "test": "node --test",
    "db:local": "wrangler d1 migrations apply cnbuycha --local",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
